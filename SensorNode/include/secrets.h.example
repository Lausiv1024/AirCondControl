#pragma once

// このファイルを secrets.h にコピーして実際の値を入れる。
// secrets.h は .gitignore で除外されているのでコミットされない。

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// AEHAFmtSender (RPi) のLAN上のアドレス。
// RPi側 deploy/aehafmtsender.service の ASPNETCORE_URLS が 0.0.0.0:5195 になっている必要がある。
#define SERVER_HOST "192.168.1.50"
#define SERVER_PORT 5195

// 複数台設置する場合に区別するためのID
#define DEVICE_ID "esp32-01"
